additionalProperties:
  formFields:
    - default: 2095
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Panel Port
      labelZh: 面板端口
      required: true
      rule: paramPort
      type: number
      label:
        en: Panel Port
        zh: 面板端口
    - default: 2096
      edit: true
      envKey: PANEL_APP_PORT_SUB
      labelEn: Subscription Port
      labelZh: 订阅端口
      required: true
      rule: paramPort
      type: number
      label:
        en: Subscription Port
        zh: 订阅端口
    - default: 2080
      edit: true
      envKey: PANEL_APP_PORT_PROXY_HTTP
      labelEn: Proxy HTTP Inbound Port
      labelZh: 代理 HTTP 入站端口
      required: true
      rule: paramPort
      type: number
      label:
        en: Proxy HTTP Inbound Port
        zh: 代理 HTTP 入站端口
    - default: 2443
      edit: true
      envKey: PANEL_APP_PORT_PROXY_HTTPS
      labelEn: Proxy HTTPS Inbound Port
      labelZh: 代理 HTTPS 入站端口
      required: true
      rule: paramPort
      type: number
      label:
        en: Proxy HTTPS Inbound Port
        zh: 代理 HTTPS 入站端口
